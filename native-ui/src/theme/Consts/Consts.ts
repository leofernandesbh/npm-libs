import { RNThemeConstsProps } from "../theme.types";

const Consts: RNThemeConstsProps = {
  UI_SIZE_MULTIPLIER: 4,
  DEFAULT_ICON_SIZE: 7,
  DEFAULT_OPACITY_CLICK: 0.8,
  DISABLED_OPACITY: 0.7,
}

export default Consts